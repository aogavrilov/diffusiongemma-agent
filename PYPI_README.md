# DiffusionGemma Agent

The canonical project and PyPI description is maintained in `README.md`.

```powershell
python -m pip install --upgrade diffusiongemma-agent
dg-agent doctor
dg-agent install --accept-licenses
```

Source: https://github.com/aogavrilov/diffusiongemma-agent
